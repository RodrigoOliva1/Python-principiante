"""Cuadrado de un binomio. Plantear un script directamente en el shell 
de Python, que permita mostrar, para dos valores 𝑎 y 𝑏, que: Un binomio 
al cuadrado (suma) es igual al cuadrado del primer término, 
más el doble producto del primero por el segundo más el cuadrado del 
segundo."""

#declaro variables
a=0
b=0
binomio=0
resultado=0

#inicializo programa
a=int(input("Ingrese un número A:"))
b=int(input("Ingrese un número B:"))
binomio=(a + b)**2
resultado=binomio =a ** 2 + 2 * ((a)*(b)) + b ** 2

print("El cuadrado de un binomio es:", resultado)